/**
 * Example of formatter function
 * Format of the new message push notification title
 * @param name - name of the message sender
 */
export const newMessagePushNotificationTitle = (name: string) =>
  `You have a new message from ${name}`;
